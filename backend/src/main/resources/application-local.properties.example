# 本地开发环境配置
# 复制此文件为 application-local.properties 并填入实际值
# 或在 IDE 中设置环境变量后激活 local profile

server.port=8080
spring.application.name=backend

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/emotion_diary?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.session_factory.inject_statement_inspector=true

# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS Configuration (for local development)
cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000}

# OAuth local mock mode.
# QQ/WeChat website login cannot be completed from localhost without real platform callbacks.
# Enable this to test the complete local flow without contacting QQ or WeChat:
# /login -> /api/auth/oauth/{provider}/authorize -> backend callback -> /oauth/callback
app.frontend-base-url=${FRONTEND_BASE_URL:http://localhost:3000}
oauth.qq.client-id=${QQ_CLIENT_ID:local-qq}
oauth.qq.client-secret=${QQ_CLIENT_SECRET:local-qq-secret}
oauth.qq.redirect-uri=${QQ_REDIRECT_URI:http://localhost:8080/api/auth/oauth/qq/callback}
oauth.wechat.client-id=${WECHAT_CLIENT_ID:local-wechat}
oauth.wechat.client-secret=${WECHAT_CLIENT_SECRET:local-wechat-secret}
oauth.wechat.redirect-uri=${WECHAT_REDIRECT_URI:http://localhost:8080/api/auth/oauth/wechat/callback}
oauth.mock.enabled=${OAUTH_MOCK_ENABLED:true}

# Redis Configuration (Redisson)
redis.host=${REDIS_HOST:localhost}
redis.port=${REDIS_PORT:6379}
redis.password=${REDIS_PASSWORD:redis123}

# MinIO Configuration
minio.endpoint=${MINIO_ENDPOINT:http://localhost:9000}
minio.access-key=${MINIO_ACCESS_KEY:minioadmin}
minio.secret-key=${MINIO_SECRET_KEY:minioadmin}
minio.bucket-name=avatars

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Activate local profile
spring.profiles.active=local
